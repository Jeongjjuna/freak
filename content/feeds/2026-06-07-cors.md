---
date: "2026-06-07T15:13:00+09:00"
tags: ["CORS", "Web", "보안"]
---

## CORS (Cross-Origin Resource Sharing)

### 한 줄 정의
`프론트 JS가 다른 출처 응답을 읽어도 되는지`를, 서버가 헤더로 허용해주는 규칙.

### SOP가 "막는다"는 게 무슨 뜻
- `SOP(동일 출처 정책)` = 브라우저에 기본 내장된 보안 규칙.
- 내용: `브라우저가, 프론트 JS가 다른 출처에서 온 응답을 읽지 못하게 차단`한다.
- 막는 주체는 내 서버가 아니라 `사용자 브라우저`. 그래서 서버끼리 통신(curl)엔 안 걸린다.
- `출처(origin)` = 스킴 + 호스트 + 포트. 하나라도 다르면 "다른 출처".

→ CORS는 서버가 응답 헤더로 `이 출처는 읽어도 됨`이라고 알려줘서 SOP 차단을 풀어주는 것.

### 서버는 요청자 출처를 어떻게 아나
브라우저가 교차 출처 요청에 `Origin` 헤더를 자동으로 붙인다.
```http
# 요청 (브라우저 → 서버)
GET /api/data
Origin: https://my-site.com

# 응답 (서버 → 브라우저)
Access-Control-Allow-Origin: https://my-site.com
```
서버는 `Origin`을 읽고 → 허용 목록에 있으면 → 그 값을 그대로 `Allow-Origin`에 담아 되돌림(`에코`).
`*` 대신 에코하는 이유: 자격증명 요청은 `*` 불가 + 여러 출처 허용 시 목록 검사 후 반사해야 하므로.

### 주요 응답 헤더
```http
Access-Control-Allow-Origin: https://my-site.com
Access-Control-Allow-Methods: GET, POST
Access-Control-Allow-Headers: Content-Type
Access-Control-Allow-Credentials: true   # 쿠키 등 자격증명 허용
```

### Preflight (사전 요청)
GET/POST(단순) 외(예: PUT/DELETE, 커스텀 헤더)면 본 요청 전에 `OPTIONS`로 미리 물어봄 → 서버가 허용 헤더로 응답해야 본 요청 진행.

### 흔한 오해
- `차단하는 건 브라우저`, 서버 아님. 요청은 서버에 이미 도달했을 수도 있음.
- `credentials: true`면 `Allow-Origin: *` 불가 → 정확한 출처 명시 필요.
- `Origin` 헤더는 브라우저가 채우고 JS로 못 바꿈(forbidden header) → 브라우저 환경에선 신뢰 가능. 단, curl 등 비브라우저는 위조 가능 → CORS는 "진짜 서버 보안"이 아님.

> `핵심` — 브라우저가 기본으로 다른 출처 응답 읽기를 막고(SOP), 서버가 `Origin` 보고 허용 헤더로 알려주면 읽게 된다(CORS).